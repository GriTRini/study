#include <stdio.h>

int main(void)
{	
	int a, b, c;
	printf(" input num : ");
	scanf("%d %d %d", &a, &b, &c);
	
	if (a > b && a > c){
		if (c > a) {
			printf("max : %d\t mid: %d\t min: %d\n", c, a, b);
		} else if (c > b) {
			printf("max : %d\t mid: %d\t min: %d\n", a, c, b);
		} else {
		   printf("max : %d\t mid: %d\t min: %d\n", a, b, c);
	} else {
		ir (c > b) {
			printf("max : %d\t mid: %d\t min: %d\n", c, a, b);
			
			return 0;
}	
