#include <stdio.h>

int main(void)
{
	int year;
	printf("input year: ");
	scanf("%d", &year);
	
	int isOdrdinary;
	//isLeap = (year % 4 == 0 && year % 100 != 0 || year % 400 == 0); 
	isOdrdinary

	printf("%d is a isOdrdinary year : %d\n", year, isOdrdinary);
	return 0;
}
